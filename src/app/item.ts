export class Item {
    constructor(public name:string, public descr:string, public img:string, public id:number){
        this.name=name;
        this.descr=descr;
        this.img=img;
        this.id=id;
    }
}
